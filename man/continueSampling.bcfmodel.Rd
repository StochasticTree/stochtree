% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcf.R
\name{continueSampling.bcfmodel}
\alias{continueSampling.bcfmodel}
\title{Continue Sampling a BCF Model}
\usage{
\method{continueSampling}{bcfmodel}(
  object,
  X_train,
  Z_train,
  y_train,
  propensity_train = NULL,
  rfx_group_ids_train = NULL,
  rfx_basis_train = NULL,
  X_test = NULL,
  Z_test = NULL,
  propensity_test = NULL,
  rfx_group_ids_test = NULL,
  rfx_basis_test = NULL,
  num_burnin = 0,
  num_mcmc = 100,
  general_params = list(),
  prognostic_forest_params = list(),
  treatment_effect_forest_params = list(),
  variance_forest_params = list(),
  random_effects_params = list(),
  ...
)
}
\arguments{
\item{object}{Fitted \code{bcfmodel} to continue sampling.}

\item{X_train}{Training covariates (re-supplied; same structure used to fit the model).}

\item{Z_train}{Training treatment assignments (re-supplied).}

\item{y_train}{Training outcome (re-supplied).}

\item{propensity_train}{(Optional) Training propensity scores. Required if the model used a propensity
covariate and no internal propensity model is available to re-derive them.}

\item{rfx_group_ids_train}{(Optional) Training random effects group labels (required if the model has rfx).}

\item{rfx_basis_train}{(Optional) Training random effects basis (required for a custom rfx model).}

\item{X_test}{(Optional) Test covariates. When supplied, test-set predictions are recomputed in full
from all retained forests, so the test set need not match any test set used in the original fit (the
model may have been fit with none). When omitted, any cached test predictions are dropped.}

\item{Z_test}{(Optional) Test treatment assignments (required when \code{X_test} is provided).}

\item{propensity_test}{(Optional) Test propensity scores. Required if the model used a propensity covariate
and no internal propensity model is available to re-derive them.}

\item{rfx_group_ids_test}{(Optional) Test random effects group labels (required when \code{X_test} is provided and the model has rfx).}

\item{rfx_basis_test}{(Optional) Test random effects basis (required for a custom rfx model when \code{X_test} is provided).}

\item{num_burnin}{Number of additional burn-in iterations to discard. Default \code{0}.}

\item{num_mcmc}{Number of additional retained MCMC draws. Default \code{100}.}

\item{general_params}{(Optional) List of changeable general parameters (e.g. \code{random_seed}, \code{keep_every},
\code{keep_burnin}, \code{cutpoint_grid_size}, \code{sigma2_global_shape}, \code{sigma2_global_scale}, \code{num_threads}, \code{verbose}).
May also include \code{variable_weights} (per-covariate split weights); if omitted, the fit-time weights are reused.}

\item{prognostic_forest_params}{(Optional) Changeable prognostic (mu) forest parameters (\code{alpha}, \code{beta},
\code{min_samples_leaf}, \code{max_depth}, \code{num_features_subsample}, \code{sigma2_leaf_shape}, \code{sigma2_leaf_scale}).
May also include \code{keep_vars} / \code{drop_vars} to change which covariates this forest may split on; if omitted,
the fit-time split-variable subset is reused.}

\item{treatment_effect_forest_params}{(Optional) Changeable treatment (tau) forest parameters (same keys as
prognostic, including \code{keep_vars} / \code{drop_vars}).}

\item{variance_forest_params}{(Optional) Changeable variance forest parameters (\code{alpha}, \code{beta},
\code{min_samples_leaf}, \code{max_depth}, \code{num_features_subsample}, \code{var_forest_prior_shape}, \code{var_forest_prior_scale},
and \code{keep_vars} / \code{drop_vars}).}

\item{random_effects_params}{(Optional) Changeable random effects parameters (\code{variance_prior_shape},
\code{variance_prior_scale} — the inverse-gamma prior on the random effects group-parameter variance).
Ignored if the model has no random effects.}

\item{...}{Other parameters (ignored).}
}
\value{
The updated \code{bcfmodel} (mutated in place; the sampled forests are extended).
}
\description{
Continue sampling from a BCF model, appending additional draws to the existing
posterior samples. The training data must be re-supplied (it is not retained on the model).
The sampler initializes every term (prognostic + treatment forests, variance forest,
random effects, tau_0, and adaptive-coding b0/b1) from its last retained sample.
}
