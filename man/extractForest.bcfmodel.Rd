% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcf.R
\name{extractForest.bcfmodel}
\alias{extractForest.bcfmodel}
\title{Extract BCF Forests}
\usage{
\method{extractForest}{bcfmodel}(object, term)
}
\arguments{
\item{object}{Object of type \code{bcfmodel} containing draws of a BCF model and associated sampling outputs.}

\item{term}{Name of the forest to extract (e.g., \code{"prognostic"}, \code{"treatment"}, \code{"variance"}).}
}
\value{
Object of class ForestSamples containing a deep copy of the requested forest samples.
}
\description{
Extract a forest from a BCF model by name.
If the requested forest type is not found, an error is thrown.
The following conventions are used for forest:
\itemize{
\item Prognostic (mu) forest: \code{"prognostic"}, \code{"prognostic_forest"}, \code{"mu"}
\item Treatment effect (tau) forest: \code{"treatment"}, \code{"treatment_forest"}, \code{"tau"}
\item Variance forest: \code{"variance"}, \code{"variance_forest"}
}

The treatment forest is the raw treatment-effect forest tau(x) (without \code{tau_0} or any
adaptive-coding scaling); for the full CATE use \code{extractParameter(object, "tau_hat_train")}.
}
\examples{
n <- 100
p <- 5
X <- matrix(runif(n*p), ncol = p)
pi_x <- 0.25 + 0.5*X[,1]
Z <- rbinom(n, 1, pi_x)
mu_x <- X[,1]*2
tau_x <- X[,2]*(-1)
y <- mu_x + tau_x*Z + rnorm(n)
bcf_model <- bcf(X_train=X, Z_train=Z, y_train=y, propensity_train=pi_x,
                 num_gfr=0, num_mcmc=10)
prognostic_forest <- extractForest(bcf_model, "prognostic")
treatment_forest <- extractForest(bcf_model, "treatment")
}
